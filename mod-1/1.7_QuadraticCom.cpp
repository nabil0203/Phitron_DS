#include <bits/stdc++.h>
using namespace std;

int main()
{
    

    // nested loop

    /*
     
    if 2 loops are nested O(n^2)
    if 3 loops are nested O(n^3)
    if 4 loops are nested O(n^4)
    
    */


    return 0;
}